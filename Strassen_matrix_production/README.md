# 📘 Strassen Matrix Multiplication — C++ Implementation

This project implements Strassen’s Matrix Multiplication Algorithm in C++ and includes:

A complete Strassen recursive multiplication implementation

Classic O(n³) matrix multiplication for comparison

Automatic matrix input generator

Support for arbitrary dimensions with padding (if dimension is odd)

Clear modular structure (split, join, add, subtract, recursion)

Performance-ready code for further benchmarking

### 🚀 Features

✔ Full implementation of Strassen’s divide-and-conquer algorithm
✔ Classic matrix multiplication for crossover comparison
✔ Automatic handling of odd-sized matrices (padding logic)
✔ File-based input for reproducibility
✔ Fast random matrix generator (matrix_input.txt)
✔ Minimal dependencies — standard C++ only
✔ Well-structured helper functions (add, subtract, split, join)

### 📂 Project Contents
```
strassen.cpp     — main Strassen implementation + classic multiplication
matrix_gen.cpp   — random matrix generator into matrix_input.txt
matrix_input.txt — autogenerated or custom test data
```

### 🧠 About Strassen’s Algorithm

Strassen reduces matrix multiplication complexity from:

O(n³) → O(n^2.807...)

using the identities:

M1 = (A11 + A22)(B11 + B22)
M2 = (A21 + A22) B11
M3 = A11 (B12 – B22)
M4 = A22 (B21 – B11)
M5 = (A11 + A12) B22
M6 = (A21 – A11) (B11 + B12)
M7 = (A12 – A22) (B21 + B22)


Combined into result blocks:

C11 = M1 + M4 – M5 + M7
C12 = M3 + M5
C21 = M2 + M4
C22 = M1 – M2 + M3 + M6


This implementation uses all these formulas exactly as described.

### 🛠️ How to Build

Compile using any modern C++ compiler:
```
Build Strassen program
g++ -O2 strassen.cpp -o strassen

Build matrix generator
g++ -O2 matrix_gen.cpp -o matrix_gen
```
### ▶️ How to Run
Step 1 — Generate matrices
./matrix_gen


This creates:

matrix_input.txt


containing the values of matrices A (first N² lines) and B (second N² lines).

Step 2 — Run Strassen multiplication
./strassen 0 <dimension> matrix_input.txt


Example:

./strassen 0 4 matrix_input.txt

📥 Input Format (matrix_input.txt)

For dimension N:
```
A[0][0]
A[0][1]
...
A[N-1][N-1]
B[0][0]
B[0][1]
...
B[N-1][N-1]


Each value is on its own line.

Example for N = 2:

1
2
3
4
5
6
7
8
```
📤 Output Format

The program prints the resulting C = A × B matrix in full:

c11 c12 ...
c21 c22 ...
...

### ⚙️ Key Implementation Details
✔ Padding

If the matrix dimension is odd, the algorithm adds a padding row/column to make it even.

✔ Crossover point

A threshold (crossover = 86) determines when to switch to classical O(n³) multiplication — improves performance on small matrices.

✔ Memory reuse

Temporary submatrices reuse storage and minimize allocations.

### 📊 Possible Improvements

Performance benchmarking (Strassen vs classic)

Parallelization (std::thread or OpenMP)

Switching to 1-D arrays for cache efficiency

Using move semantics to reduce vector copies

Profiling with chrono timers (already partially included)

### 🎯 Purpose of the Project

This code demonstrates:

Understanding of divide-and-conquer algorithms

Recursive algorithms

Matrix operations

Memory-efficient use of std::vector
Real implementation of a sub-cubic matrix multiplication algorithm

It is suitable both as an academic demonstration and as a portfolio project
