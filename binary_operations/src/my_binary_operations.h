#ifndef BINARY_H
#define BINARY_H

#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

void add_one(std::string &bin);

std::string sum_and_carry(std::string sa, std::string sb, char op);

#endif // BINARY_H